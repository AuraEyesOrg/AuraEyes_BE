faro.receiver "integrations_faro" {
  server {
    listen_address = "0.0.0.0"
    listen_port = 12347
    cors_allowed_origins = ["*"]
  }
  output {
    logs   = [loki.process.faro_logs.receiver]
    traces = [otelcol.exporter.otlp.tempo.input]
  }
}

loki.process "faro_logs" {
  forward_to = [loki.write.grafana_loki.receiver]

  stage.static_labels {
    values = {
      "job" = "faro",
    }
  }

  // Faro gửi data lồng trong object 'meta'
  stage.json {
    expressions = {
      "app_name" = "app.name",
      "app_env"  = "app.environment",
      "browser"  = "browser.name",
      "kind"     = "kind",
    }
    source = "meta"
  }

  stage.labels {
    values = {
      "app"         = "app_name",
      "application" = "app_name",
      "service_name"= "app_name",
      "env"         = "app_env",      // Đổi thành 'env' để khớp với biến Dashboard
      "environment" = "app_env",      // Giữ lại cái này cho chắc
      "browser_name"= "browser",
      "kind"         = "kind",
    }
  }
}

loki.write "grafana_loki" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

otelcol.exporter.otlp "tempo" {
  client {
    endpoint = "tempo:4317"
    tls {
      insecure = true
    }
  }
}
